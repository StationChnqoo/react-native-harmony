import React, {useEffect} from 'react';
import PropTypes from 'prop-types';

import {
  Dimensions,
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  FlatList,
} from 'react-native';

export interface ListViewProps {}

const Button: React.FC<ListViewProps> = props => {
  return <View></View>;
};

const defaultStyles = StyleSheet.create({});

export default Button;